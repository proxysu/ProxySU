<UserControl x:Class="ProxySuper.WPF.Controls.VLESS_XTLS_RAW_REALITY_Control"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProxySuper.WPF.Controls"
             mc:Ignorable="d">
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <Label Content="地址(address)" Width="140" />
            <TextBox Text="{Binding Settings.Domain}" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="端口(port)" Width="140" />
            <TextBox Text="{Binding Settings.Port}" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="用户ID(id)" Width="140" />
            <TextBox Text="{Binding Settings.UUID}" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="流控(flow)" Width="140" />
            <TextBox Text="{Binding Settings.Flow}" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="加密(encryption)" Width="140" />
            <TextBox Text="none" IsReadOnly="True" Width="300" />
        </StackPanel>



        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="传输协议(network)" Width="140" />
            <TextBox Text="tcp(raw)" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="伪装类型(type)" Width="140" />
            <TextBox Text="none" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="伪装域名(host)" Width="140" />
            <TextBox Text="{Binding Settings.MaskDomain}" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="路径(path)" Width="140" />
            <TextBox Text="" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="传输安全(tls)" Width="140" />
            <TextBox Text="reality" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="SNI" Width="140" />
            <TextBox Text="{Binding Settings.MaskDomain}" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="Fingerprint" Width="140" />
            <TextBox Text="{Binding Settings.UTLS}" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="PublicKey" Width="140" />
            <TextBox Text="{Binding Settings.REALITY_publicKey}" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="ShortId" Width="140" />
            <TextBox Text="" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="SpiderX" Width="140" />
            <TextBox Text="{Binding Settings.REALITY_spiderX}" IsReadOnly="True" Width="300" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Label Content="分享链接(ShareLink)" Width="140" />
            <TextBox Text="{Binding Settings.VLESS_RAW_XTLS_REALITY_ShareLink,Mode=OneTime}" Width="300" IsReadOnly="True" />
        </StackPanel>
    </StackPanel>
</UserControl>
